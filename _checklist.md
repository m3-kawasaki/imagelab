# ImageLab 画面確認チェックリスト

作成：2026-09-02　／　更新：2026-09-10

> **これは開発用の記録です。**人の目でしか確かめられないこと（見え方・操作感）を
> 48項目に落として、1つずつ確認した記録が残してあります。
> ロジックの検査は `docs/_selftest.js`（664項目）と `_selfcheck.html`（22処理の総当たり）が担当です。
>
> 手順の「開き方」欄は `index.html#…` のハッシュURLです。公開ページで開くなら
> `https://m3-kawasaki.github.io/imagelab/` のうしろにハッシュを付けてください。
> なお、一部の項目が引き合いに出している手元の写真は**同梱していません**
> （同梱サンプル9枚で追える形にしてあります）。

| 範囲 | 対象 | 状態 |
|---|---|---|
| **項目 1〜10**（周波数領域） | `js/ops/fft.js` `fftfilter.js` `fftmask.js`／`js/fft.js` | **2026-09-04 に 1〜10 すべて合格** |
| **項目 11〜18**（ノイズと評価・輝点の案内） | `js/ops/noise.js` `denoisecmp.js`／`viewer.js` の見出し／`js/fft.js` の輝点さがし | **2026-09-06 に 11〜18 すべて合格**（13・16 は 09-04 に期待値の側を修正済み） |
| **項目 19〜24**（二値化） | `js/ops/threshold.js` | **2026-09-06 に 19〜24 すべて合格** |
| **項目 25〜30**（モルフォロジー） | `js/ops/morphology.js` | **2026-09-06 に 25〜30 すべて合格**（29 は測り方を三度直したうえで、3回繰り返しても安定することを確認） |
| **項目 31〜36**（デモザイク） | `js/ops/demosaic.js` | **2026-09-07 に 31〜36（33b を含む7項目）すべて合格**（31・32・34 は手順の側を書き直したうえでの合格） |
| **項目 37〜42**（幾何変換） | `js/ops/affine.js` `lensdist.js`／`js/core.js` の `U.sampleImage` | **2026-09-07 に 37〜42 すべて合格** |
| **項目 45〜48**（周期ノイズの付加） | `js/ops/noise.js`／`js/core.js` の `U.noiseBand` | **2026-09-10 に 45〜48 すべて合格**（45 のサンプルを `geometry` → `gaussnoise` に差し替えたうえでの合格） |

# ■ 2026-09-10、画面確認は全48項目そろいました

**未実施はありません。**以下はすべて記録です。
設計書の第1〜第3階層の実装と画面確認は 2026-09-07 に終わり、
そのあと足した**設計書に無い「周期ノイズの付加」**も 2026-09-10 に確認が済みました。

| 範囲 | 項目数 |
|---|---:|
| 1〜10（周波数領域） | 10 |
| 11〜18（ノイズと評価。16b を含む） | 9 |
| 19〜24（二値化） | 6 |
| 25〜30（モルフォロジー） | 6 |
| 31〜36（デモザイク。33b を含む） | 7 |
| 37〜42（幾何変換） | 6 |
| 45〜48（周期ノイズの付加） | 4 |
| **合計** | **48（全部合格）** |

**途中で「期待値の側」を直したもの（5件）。**どれも op の不具合ではなく、
私の書いた確認手順が誤っていました。次に項目を足す人への申し送りです。

| # | 何が誤っていたか | 教訓 |
|---|---|---|
| **13・16** | 「印が出ないサンプル」「★の付き方」の前提が違った | 教材の主張はサンプルを何枚か回して確かめる |
| **29** | 測り方（測る区間・走らせる場所・測る回数）が3段階とも誤り | 画面に出す数字をベンチマークに使うなら、ベンチマークの作法で測る |
| **31・32** | 4分割タイル（半分の解像度）で1画素単位の偽色を見ようとしていた | 4分割は数値を比べる表示。粒は全解像度＋差分表示＋ルーペで見る |
| **34** | ノイズの無いサンプルで「ノイズが消えるのを見る」と書いていた | 効果を見せる項目は、効果が大きく出るサンプルを**数値で選んでから**書く |
| **45** | **白飛びする絵（`geometry`）に縞を乗せていた。**飽和して倍音が生まれ、印が6個出た | **足すノイズの振幅と、台の明るさの余裕を合わせて確かめる**（セルフテストには飽和の注意を書いていたのに、サンプル選びに反映していなかった） |

**共通する反省：自分で画面を見られない以上、期待値は書く前に Node で数値の裏を取ること。**
**5件のうち4件が「サンプルの選び方」の誤り**でした。op を書いたら、
**その効果がいちばん大きく出る絵を数値で選ぶところまでが仕事**です。

---

## 前提

### 自動チェック

`node docs/_selftest.js` … **664 項目 合格**（2026-09-10。周期ノイズの追加で 641 → 664）

`_selfcheck.html` … **2026-09-06 に取り直し済み。処理ボタン 22 個・例外なし。**
幾何変換の2処理を含めた全処理を通っています。

> **2026-09-10 の追加では、処理ボタンの数は 22 のままです。**新しい op を足したのではなく、
> 既存の〈ノイズ付加〉に選択肢を1つ増やしただけなので、`index.html` と `_selfcheck.html` への
> `<script>` の追記は要りません。取り直しても 22 個のままが正常です。

> **経緯（次に触る人向け）。**2026-09-06 の1回目に頂いた「例外なし」は、
> `_selfcheck.html` が**二値化とモルフォロジーの2ファイルを読み込んでいなかった**
> ため、その2つを素通りしたものでした（`<script>` の追記漏れ）。
> 同日に追記して 20処理で取り直し、さらに幾何変換の2処理を足して
> **22処理で取り直し、例外なしを確認しています。**
> **処理を足したら `index.html` と `_selfcheck.html` の両方に書くこと。**
> 左上の「処理ボタン N 個」が `index.html` の処理数と合っているかで確かめられます。

以下は**その自動チェックでは見られない「画面の見え方と操作感」**の確認です。

### 時間の見せ方を直しました（2026-09-06）

項目29 のご指摘を受けての修正です。**3段階で直しました。**

**1. 時間の行を2つに分けた。**

| 行 | 中身 | 使いどころ |
|---|---|---|
| **計算時間** | 処理の中身（`prepare` と `kernel`）だけを測った合計 | **処理どうしを比べるのはこちら** |
| **画面に出るまで** | 結果が返るまでの実時間 | 体感の待ち時間。並列で走ると計算時間より小さくなる |

以前の「処理時間」は後者だけを出していました。軽い処理は順番待ちのあいだに
直前の描画が割り込むため、**50 ms の処理が 200 ms と出たり**します。

並列で走ったときは「計算時間」に **（8 並列の合計）** のような但し書きが付きます。
合計が実時間より大きいのは、手分けしたぶんだけ短くなっているという意味です。

**2. 処理時間を見せる4つの処理は、ベンチマークと同じ測り方にした。**

対象は **二値化・モルフォロジー・アフィン変換・レンズ歪み**の4つです。
1 だけでは足りませんでした（詳しくは項目29 の欄）。

- **軽くても必ず Worker で計算する** … 「実行」欄はいつも `Worker 1 並列`
- **1回空回ししてから2回まわし、速かったほうを採る** …
  「計算時間」に**（2回の最小）**という但し書きが付き、
  **「画面に出るまで」は計算時間の3倍ほど**になります（3回まわすので正常です）

速くするためではなく、**同じ条件で測るため**です。
ほかの処理は今までどおり1回だけ計算します。

### ハッシュ URL の使い方

`index.html` をダブルクリックして開いたあと、アドレスバーの末尾に
下の表の「開き方」の文字列を貼り付けます。

> **ハッシュだけを書き換えたときは、Enter のあと `F5` で再読み込みしてください。**
> ページの読み込み時にしかハッシュを読まないので、Enter だけでは切り替わりません。

> **2026-09-06 変更。**「開き方」の欄は**バッククォートを外して素の文字列にしました**。
> そのままドラッグしてコピーできます（以前はバッククォートが `%60` として URL に
> 混ざり、別のサンプルが開いてしまいました）。

ハッシュを使わず、**左の処理リストから選び、上の「サンプル」から選ぶ**のでも同じです。
#op=fft&sample=zone は「左で〈FFT スペクトル〉、サンプルで〈ゾーンプレート〉」の意味です。

---

## 確認項目 1〜10（周波数領域）── 2026-09-04 に全項目合格ずみ

以下は記録です。**やり直す必要はありません。**

| # | 開き方 | 操作 | 合格の見え方 |
|---|---|---|---|
| **1** | #op=fft&sample=zone | そのまま眺める | スペクトルが**同心円のリング**になる。中央に十字と、目盛りの円が3本重なって見える |
| **2** | （1のまま） | 右の「境界処理」を**ゼロで埋める**に変える → 次に **Hann 窓**に変える | ゼロ詰めで縦横に**強い十字**が現れる。Hann 窓にすると**その十字が消える** |
| **3** | （1のまま。境界処理は鏡映に戻す） | 「表示」を**位相だけで再構成**にする → 次に**振幅だけで再構成**にする | 位相だけ＝**輪郭が読み取れる絵**になる／振幅だけ＝**中央に光の塊**が出るだけで何が写っていたか分からない。この差がはっきり出ること |
| **4** | #op=fftmask&sample=periodic | 右パネルを見る | 右パネルに**スペクトルが横長で描かれる**（画像と同じ 4:3）。中央に十字と目盛りの円。中心から離れた位置に**孤立した輝点が1組**見える |
| **5** | （4のまま） | 右パネルの**その輝点をドラッグして塗る** | 赤い丸が乗り、**反対側（点対称の位置）にも同時に赤い丸が乗る**。0.5秒ほど待つと、中央の画像から**斜め縞だけが消えて下の絵が残る** |
| **6** | （5のあと） | 塗った場所を**右ドラッグ**する | 赤が消えて縞が戻る。**右クリックメニューが出ない**こと |
| **7** | （4のまま） | プリセットを5個とも押す（全通過／ローパス／ハイパス／バンドパス／十字を落とす） | ローパスは外周が赤くなって画像がぼける、ハイパスは中心が赤くなって輪郭だけになる、など。**5個とも押せて、押すたびに絵が変わる**こと |
| **8** | （4のまま） | 右の「ビューアに出すもの」を**マスクを掛けたあとのスペクトル**にする | 中央の**大画面にスペクトル**が出る。遮った場所が赤くなっていて、右パネルと同じ形に見える |
| **9** | #op=fftfilter&sample=fineline | 「切り方」を**理想**にして、「しきい周波数」を 0.06 → 0.02 あたりまで下げる → 次に「切り方」を**ガウシアン**に変える | 理想では輪郭のまわりに**同心円の波紋**（リンギング）が出る。ガウシアンに変えると**波紋が消える** |
| **10** | #op=fftfilter&sample=periodic | 右下の「実行」欄を見る | **`Worker 1 並列`** と出る。FFT は画像を帯に分けられないので、**1 が正常**です（メディアンなどが 4〜8 並列になるのとは別扱い） |

**項目4について頂いた指摘**（2026-09-04）：
「孤立した輝点がどれか、初見では分からなかった」。
→ **項目11 で対応を確認し、2026-09-06 に合格。**

---

## 確認項目 11〜18（ノイズと評価）── 2026-09-06 に全項目合格ずみ

以下は記録です。**やり直す必要はありません。**

| # | 開き方 | 操作 | 合格の見え方 |
|---|---|---|---|
| **11** | #op=fftmask&sample=periodic | 右パネルを見る（項目4と同じ画面） | **黄色い破線の丸**が輝点の上に出て、いちばん強いものに「**ここ**」と添えられている。**丸の中を塗ると、その丸が消える**（狙った場所を押さえられた合図）。右ドラッグで戻すと丸が復活する |
| **12** | （11のまま） | 「**目立つ輝点に印をつける**」のチェックを外す → 入れ直す | 印が消える／戻る。**一度ページを閉じて開き直しても、外した状態が残っている**こと |
| **13** | #op=fftmask&sample=gaussnoise | 右パネルを見る | **印が1つも出ない**（ノイズと段差だけで周期的な模様が無いので、出ないのが正解です。エッジとノイズが多い＝誤検出しやすい条件をわざと選んでいます） |
| **14** | #op=noise&sample=step | 「種類」を3つとも切り替える | 3種類とも見た目が変わる。右下に **PSNR と SSIM** の行が出る。**ショットだけ、上の段（暗いところ）が明るいところより荒れる** |
| **15** | （14のまま。種類はガウシアン） | 「乱数の種」を動かす／「色ごとに独立に乗せる」を入れる | 種を変えると粒の出方が変わる。色を入れると**カラフルなざらつき**になる |
| **16** | #op=denoisecmp&sample=geometry | そのまま眺める | **4分割**で「ノイズあり／メディアン／バイラテラル／ガウシアン」が並ぶ。各面の左上に**見出しと数値**が乗る。**★は PSNR の列に1つ・SSIM の列に1つ**（この絵では2つともバイラテラルに付きます）。右下にも**見出し＋4手法の5行**が並び、**下端まで全部読める** |
| **16b** | #op=denoisecmp&sample=siemens | そのまま眺める | ★が**別の面に分かれる**（PSNR はバイラテラル／SSIM はガウシアン） |
| **17** | （16のまま） | 「ノイズの種類」を**ソルト＆ペッパー**に変える | **★が2つともメディアンに集まる**。バイラテラルの面は**ほとんどノイズが残ったまま**（これが正常です） |
| **18** | （17のまま） | 「表示」を**メディアン（全解像度）**に変える | 4分割が解けて1画面になり、**見出しが消える**。分割スライダーで原画と比べられる。**表示を切り替えても右下の4行はそのまま出ている** |

### ★の付き方と、印が出るサンプルについて（2026-09-04 追記）

**どの手法に★が付くかは絵によって変わります。**「PSNR はバイラテラル／SSIM は
ガウシアン」は既定の姿ではなく、`siemens` で起きる一例でした。既定パラメータでの実測：

| 絵 | PSNR★ | SSIM★ |
|---|---|---|
| `siemens` | バイラテラル | **ガウシアン**（分かれる） |
| `geometry` / `gaussnoise` / `saltpepper` | バイラテラル | バイラテラル |
| `step` / `lowcontrast` | ガウシアン | ガウシアン |
| `zone` | メディアン | メディアン |
| `colorbars` | メディアン | ガウシアン |

`geometry` では**ガウシアンの PSNR がノイズありを下回ります**（22.0 dB < 23.6 dB）。
線画なので、σ=1.5 のぼかしが細線とエッジを壊す損失のほうが、消したノイズの得より
大きいためです。それでも SSIM は 0.850 と高い（面積の大半を占める平坦な背景が
滑らかになるため）。**PSNR と SSIM の食い違いの一番強い実例**なので、そのまま残しました。
`zone` でも同じことが起きます（ガウシアン 17.0 dB < ノイズあり 22.8 dB）。

**印（輝点の案内）が出るサンプルと出ないサンプル**も実測してあります。

| 出る | 出ない（0個） |
|---|---|
| `lowcontrast` 6個／`step` 6個 | `gaussnoise`／`zone`／`saltpepper`／`siemens`／`colorbars` |

`lowcontrast` に印が出るのは**正しい動き**です。この絵は CLAHE で暗部から出すための
ディテールとして、32px の市松・斜めの縞・格子の3つを**わざと入れてあります**
（`js/samples.js` の 79〜81 行）。印の位置 u = ±0.328、v = ±0.016 は 64px 周期の
1/64 と 21/64 ちょうどで、市松の倍音を正しく指しています。
`step` の6個も、パッチ内に入れてある ±5 の市松（4px 周期）です。

---

## 確認項目 19〜24（二値化）── 2026-09-06 に全項目合格ずみ

以下は記録です。**やり直す必要はありません。**

> **22 は、そのあと経路を Worker に固定しました**（項目29 の欄）。
> 絵も数値も変わりませんが、「実行」欄の表示が `メインスレッド` →
> `Worker 1 並列` に変わっています。2026-09-06 に再確認ずみ。

| # | 開き方 | 操作 | 合格の見え方 |
|---|---|---|---|
| **19** | #op=threshold&sample=geometry | そのまま眺める | 白黒2値だけの絵になる。図形が黒、背景が白。右下に**閾値／分離度 η／大津なら／白の割合**の行が出る（**下端まで全部読める**こと） |
| **20** | （19のまま） | 「閾値の決め方」を**固定**にし、閾値スライダーを 0 → 255 まで動かす | **分離度 η が山なりに動く**。η が最大になるところを手で探して「大津」に戻すと、**同じ閾値**になる（右下の「大津なら」の値と一致） |
| **21** | #op=threshold&sample=lowcontrast | 「大津」と「適応的」を切り替える | **大津では左上が黒く、右下が白く潰れる**。**適応的にすると、どちらの側の細かい模様も同じように出る**。これがこの処理の山場です |
| **22** | （21のまま。適応的） | 「窓の半径」を 3 → 60 まで動かす。そのあと「窓の測り方」を**ガウシアン加重**にして同じことをする | **平均のほうは「計算時間」がほとんど変わらない**（640×480 でどちらも 15 ms 前後）。**ガウシアン加重は半径を上げると重くなる**（84 ms 前後）。積分画像の効き目がそのまま見えます。※09-06 に経路を Worker に固定したので、「実行」欄は `Worker 1 並列` になりました（項目29 の欄を参照） |
| **23** | （21のまま。適応的・平均） | 「差の下駄 C」を 0 にする → 6 に戻す | C=0 で**平坦なところにごま塩のような模様**が出て、上げると**まとめて白に倒れて**消える |
| **24** | （どれでもよい） | 「白黒を入れ替える」を入れる／外す | ちょうど反転する。右下の「白の割合」も裏返る（例：38.2% ⇄ 61.8%） |

---

## 確認項目 25〜30（モルフォロジー）

**2026-09-06 に 25〜30 すべて合格ずみ。以下は記録です。**
（29 は測り方を三度直したうえで合格。経緯は下の欄に残してあります。）

**先に知っておいてください：この処理は「白」をふくらませます。**
`geometry` は白い紙に黒い図形なので、既定のまま膨張を選ぶと**図形のほうが痩せます**。
〈白黒を入れ替える〉を入れると図形が白になり、今度は図形がふくらみます。
**これは不具合ではなく、モルフォロジーの双対性そのもの**です（項目26で確かめます）。

| # | 開き方 | 操作 | 合格の見え方 |
|---|---|---|---|
| **25** | #op=morphology&sample=geometry | 「操作」を膨張 ⇄ 収縮で切り替える | 白黒2値の絵になり、**膨張で白い紙が広がって図形が痩せ、収縮で図形が太る**。右下に**操作／構造要素／反復／白の割合（前 → 後）**が出て、膨張で白が増え、収縮で減る |
| **26** | （25のまま。膨張） | 〈白黒を入れ替える〉を入れる | **図形が白くなり、今度は図形がふくらむ**。膨張と収縮が入れ替わって見えること（双対性） |
| **27** | #op=morphology&sample=saltpepper | 「操作」をオープニング ⇄ クロージングで切り替える | **オープニングは白い粒だけ**、**クロージングは黒い粒だけ**を消す。もう片方の粒は残ったまま |
| **28** | （27のまま。オープニング） | 「反復回数」を 1 → 2 → 3 と上げる | **絵が変わらない**（冪等）。そのあと操作を膨張にして反復を上げると、**今度は進む** |
| **29** | #op=morphology&sample=geometry | 「構造要素の形」を四角 → 十字 → 円 と切り替え、半径を 8 にする（反復1・大津オン） | 見た目が変わる（四角は角ばる／円は等方／十字は斜線に効きが弱い）。**右下の「計算時間」で、四角と十字がほぼ同じ（数十 ms）／円だけが5〜6倍**（分離できないため）。**「実行」欄は3つとも `Worker 1 並列`**、計算時間に**（2回の最小）**の但し書き。**同じ形を何度切り替えても、計算時間はだいたい同じ数字に落ち着く**こと |
| **30** | #op=morphology&sample=lowcontrast | 〈先に二値化する〉を**外し**、操作をトップハット、半径を 8 に | グレースケールのまま処理され、**大きな明暗のムラが消えて細かい模様だけが残る**。照明ムラの除去そのものです |

### 項目29 について（2026-09-06 に**三度**直しました）

**画面を見た人の最初の見立てが、そのまま正解でした。**
「分離できる形は軽いと見積もられて Worker に回らず、画面の描画などと同じ場所で
走るために遅く・ばらついているのでは」——そのとおりです。
1回目・2回目とも、原因の一部しか潰せていませんでした。

| | いただいた実測 | 分かったこと | 直したこと |
|---|---|---|---|
| **1回目** | 四角 201／十字 80／円 173 ms | 測っていたのが計算時間ではなく**結果が返るまでの実時間**で、順番待ちの描画まで足されていた | 時間の行を**「計算時間」と「画面に出るまで」に分けた** |
| **2回目** | 四角 192／十字 198〜71／円 154〜188 ms | **メインスレッドで計算していること自体**が原因。ページの大きなデータを抱えたまま走るのでごみ集め（GC）が挟まる | 軽くても**必ず Worker で計算する**ようにした（`route: 'worker'`） |
| **3回目** | 四角 30〜15／十字 152〜95／円 192〜447 ms | 順番（四角 < 十字 < 円）は正しくなったが、**同じ経路のまま2倍前後ぶれる**。しかも本来ほぼ同じはずの十字が四角の5〜6倍。**1回きりの計測が当てにならない**（ワーカーが作られた直後で最適化が効いていない／PC が別の仕事で混んでいる） | **1回空回ししてから2回まわし、速かったほうを採る**（`bench: 2`） |

> **お詫びと訂正。**3回目の説明で最初「経路固定前の四角 192 ms と固定後の 15 ms は
> 同じ計算で13倍差」と書きましたが、**これは勘違いでした**（画面確認での指摘で判明）。
> 192 ms はメインスレッド、15 ms は Worker で、**実行経路そのものが違います**。
> その13倍は「経路を固定したら速くなった」＝2回目の修正が効いた証拠であって、
> 残っていた不安定さの証拠にはなっていません。
> **根拠にできるのは、経路をそろえたあとの表の中だけ**です（上の2倍前後と、
> 十字と四角の食い違い）。`bench: 2` を入れた結論は変わりませんが、理由づけを直しました。

**裏取り。**同じ計算を Node で、毎回まっさらなワーカーに渡して測りました。

```
1回きり     四角 67ms  十字 57ms  円 301ms   ← 冷えたまま測っている
空回し＋2回 四角 38ms  十字 41ms  円 289ms   ← 安定する
```

**これは `node docs/_bench.js` がやっていることと同じ**です（1回空回ししてから
3回計測してその中央値）。最小値を採るのは、**「じゃまが入らなければこの速さ」**を
指すからで、GC や別プロセスの割り込みに強い測り方です。

**そのため 29 では、こう見えます。**

- **「実行」欄は四角・十字・円のどれでも `Worker 1 並列`**（直す前は四角と十字だけ「メインスレッド」）
- **「計算時間」に「（2回の最小）」という但し書きが付く**
- **「画面に出るまで」は計算時間の3倍ほど**になります（3回まわしているので正常です）

対象は処理時間を見せる4処理（**二値化・モルフォロジー・アフィン変換・レンズ歪み**）だけで、
ほかの処理は今までどおり1回だけ計算します。

**期待値の根拠**（`node docs/_bench.js 640 480` の実測。既定の大津オン・膨張・反復1）：

| 構造要素 | 半径8 の実測 | 見積り（`op.cost`） |
|---|---:|---:|
| 四角 | 50 ms | 52 ms |
| 十字 | 51 ms | 52 ms |
| **円** | **305 ms** | 262 ms |

**絶対値はブラウザと Node で違って構いません。**`_bench.js` はグラデーションとノイズの絵で
測っていますが、`geometry` は二値化されて 0 と 255 だけになるため、
最大・最小を取るループの分岐がよく当たり、ブラウザのほうが**速く出ます**
（四角が 15〜30 ms 前後になっても正常です）。

**見ていただきたいのは絶対値ではなく比です。**

1. **四角と十字がほぼ同じ**（どちらも「横 → 縦」に分けられるので）
2. **円だけが5〜6倍**（分けられないので窓の面積ぶん回る）
3. **同じ形を何度切り替えても、数字がだいたい同じところに落ち着く**

3 がいちばん大事です。ここが崩れているようなら、まだ測り方の問題が残っています。

---

---

## 確認項目 31〜36 ── デモザイク（2026-09-07 に全項目合格ずみ）

以下は記録です。**やり直す必要はありません。**

**先に知っておいてください。**この処理は「原画をいったん Bayer 配列（1画素1色）に
間引いてから、2つのやり方でカラーに戻し、原画と比べる」ものです。
ノイズ除去の比較とまったく同じ形で、正解を自分で用意しています。

| # | 開き方 | 操作 | 合格の見え方 |
|---|---|---|---|
| **31** | #op=demosaic&sample=zone | そのまま眺める | **4分割**で「原画／Bayer 生データ／バイリニア／AHD」が並び、各面の左上に見出しが乗る。Bayer の面は**赤緑青のモザイク**。右下に **PSNR / SSIM の2行＋偽色（色差の誤差）＋ CFA の並び** が出て、**下端まで全部読める**。★は PSNR・SSIM とも AHD 側（26.4 → 37.3 dB、偽色 4.35 → 1.03）。**この画面では数値だけ見てください**（理由は下の欄） |
| **32** | #op=demosaic&sample=colorbars | 「表示」を **Bayer 生データ（全解像度）** にし、**ルーペの倍率を 16 以上**に上げて、**白い帯**（色が一様なところ）に当てる | 2×2 の並びが読み取れる。**RGGB なら 左上＝赤／右上と左下＝緑／右下＝青**。「Bayer の見せ方」を **素の値（グレー）** に変えると色が消えて灰色になる。**細かい模様のところに当てると読み取れません**（下の絵とモザイクが混ざるため） |
| **33** | #op=demosaic&sample=zone | 「表示」を **バイリニア（全解像度）** にして、**キー `4`（差分表示）** を押す。そのあと表示を **AHD（全解像度）** に切り替える | **バイリニアでは、外側の細かいリングのところに網目状の誤差が広がって見える**。AHD にすると**同じ場所の誤差が目に見えて減る**。差分の倍率スライダー（既定 ×4）を上げると弱い誤差も拾えます |
| **33b** | （33のまま） | キー `2`（処理後）に戻し、**バイリニア（全解像度）** でルーペを外側のリングに当てる | 白黒の縞のはずのところに**緑やピンクの色（偽色）が乗っている**。AHD に切り替えると薄くなる |
| **34** | #op=demosaic&sample=saltpepper | 「表示」を **AHD（全解像度）** にして、「AHD の後処理（色差メディアン）」を **0 → 1 → 3** と上げる | **0 では飛んだ画素が「色のついた点」になっていて、上げるほど色だけが抜けて白黒の点になる**。右下の「偽色（色差の誤差）」の AHD 側が **6.08 → 3.41 → 1.86** と下がる。**白黒の粒の数は変わらない**（色差にだけ効くため） |
| **35** | #op=demosaic&sample=colorbars | そのまま眺める（4分割） | **ここだけ★がバイリニア側に付く**（33.6 dB 対 31.8 dB）。AHD が負ける唯一のサンプルです。色のはっきりした縦の境目でだけ AHD の補正が行き過ぎるためで、**正常な動き**です |
| **36** | #op=demosaic&sample=siemens | 「CFA の並び」を RGGB → BGGR → GRBG → GBRG と切り替える | **どれを選んでも絵が破綻しない**（全体が変な色に転ばない）。右下の PSNR は 36.9〜37.7 dB の範囲で少しだけ動く。「CFA の並び」の行も追随する |

**31・32・34 は、最初に書いた手順では確認できませんでした。**
どれもサンプルや表示の選び方が悪く、「効果が出ない画面」を指していたためです。
差し替えた理由と実測値を、次の2つの欄に残してあります。

### 34 のサンプルを `saltpepper` に変えた理由（2026-09-07）

**最初、項目34 を「33 のまま（＝`zone`）で色差メディアンを 0 → 1 → 3」と書いていましたが、
これも私の誤りでした**（「偽色の確認ができません」とご指摘いただき判明）。

`zone` は**ノイズの無い絵**なので、AHD の段階ですでに強い偽色が残っていません
（色差の誤差の最大が 14 階調、20階調を超える画素は **0 個**）。
**見るものが残っていない**のに「上げると消える」と書いていました。

`saltpepper` に変えると、こうなります（640×480 の実測）。

| 色差メディアン | 偽色 平均 | 20階調を超える画素 |
|---|---:|---:|
| 0 回 | 6.08 | **20,885**（画面の 6.8%） |
| 1 回 | 3.41 | 3,172 |
| 3 回 | 1.86 | **482** |

飛んだ画素はそこで1色しか測れていないので、戻すと**色のついた点**になります。
それがメディアンで消えます。**しかも白黒の粒はそのまま残ります**
（輝度の誤差は 6.86 → 5.59 とほとんど動きません）。
「色ノイズだけ消して解像感は落とさない」という、この後処理の狙いがそのまま見えます。

### 31 を「数値だけ」にした理由（2026-09-06 に書き直しました）

**最初、項目31 を「4分割のバイリニアの面に偽色が浮くのを見る」と書いていましたが、
これは書き方が誤りでした**（画面確認で「ほとんど判別できません」と指摘されて判明）。
理由は2つあります。

1. **4分割は各面を 2×2 の箱平均で半分に縮めている。**その平均そのものが偽色を薄めます。
   実測で、色差の誤差の**平均が縮める前の 45〜58% に落ちます**。
2. **`siemens` はもともと偽色が弱い。**色差の誤差の平均が 1.09 で、
   `zone`（4.35）の 1/4 しかありません。しかも中心のごく狭い範囲に集中しています。

**4分割は「どちらが良いか」を数値で一目で示すための表示で、粒を見るための表示ではありません。**
粒を見る役は 33・33b に分けました。サンプルも偽色の強い `zone` に変えてあります。
解説文（右パネル）にも同じことを書き足しました。

---

## 確認項目 37〜42 ── 幾何変換（2026-09-07 に全項目合格ずみ）

以下は記録です。**やり直す必要はありません。**
第3階層の最後で、同じ「センサと幾何」カテゴリの2番目・3番目にあります。

**先に知っておいてください。**どちらも**逆写像**で回しています。
「元の画素をどこへ飛ばすか」ではなく、**出力の1画素ごとに「元の絵のどこを読むか」**を
求める形です。読む場所は小数（1.7行目の3.2列目、など）になるので、そこに画素はありません。
周りの画素から作るのが**補間**で、その3種を切り替えて見比べるのが山場です。

**用語：タップ数**

**出力の1画素を作るのに、元の絵から何点読むか**です。右下の「補間」の行に出ます。

| 補間 | 読む点 | タップ数 |
|---|---|---|
| 最近傍 | 一番近い1点だけ | **1** |
| バイリニア | 周りの 2×2 | **4** |
| バイキュービック | 周りの 4×4 | **16** |

1画素あたりの手間が **1 : 4 : 16** なので、**処理時間もおおむねその比になります**
（項目38 で確かめます）。

| # | 開き方 | 操作 | 合格の見え方 |
|---|---|---|---|
| **37** | #op=affine&sample=geometry | そのまま眺める（回転20°） | 図形が反時計回りに 20° 傾き、**四隅が黒く欠ける**。右下に**変換行列／平行移動／画像の外を読んだ画素（12.3 %）／補間（バイリニア：1画素あたり 4 点を読む）**が出る。「実行」欄は `Worker 1 並列` |
| **38** | （37のまま） | 「補間」を 最近傍 → バイリニア → バイキュービック と切り替え、斜線か円の輪郭に**ルーペ**を当てる | **最近傍では輪郭が階段状に割れ**、バイリニアで滑らかになり、バイキュービックで少し締まる。右下の「計算時間」も **16 → 22 → 66 ms** と上がる。**読む点の数（1 → 4 → 16）にだいたい比例している**のが見どころです。右下の「補間」の行にも「1画素あたり N 点を読む」と出るので、そこと突き合わせてください |
| **39** | （37のまま） | 〈往復させて誤差を見る〉を入れ、補間を3つとも切り替える | 絵はほぼ元に戻る。右下に**往復後の PSNR（重なった範囲だけ）／比べた範囲**が出て、**最近傍 30 dB 前後 ＜ バイリニア 31 dB 前後 ＜ バイキュービック 35 dB 前後**の順になる。「比べた範囲」は 87.6 % |
| **40** | （39のまま） | 回転角を **90°** にする | **往復後の PSNR が「∞」**になる。読む位置が画素の真上に乗って補間が働かないためで、どの補間を選んでも ∞ です。角度を 89° に戻すと有限に落ちる |
| **41** | #op=lensdist&sample=geometry | k₁ を −0.5 → 0 → +0.5 と動かす | 重ねた**格子の線が、マイナス側では外へふくらみ（樽型）、プラス側では内へへこむ**（糸巻き型）。k₁ = 0 でまっすぐに戻る。右下の「すること」欄も 樽型／歪みなし／糸巻き型 と追随する |
| **42** | （41のまま） | 「すること」を〈歪ませる〉→〈補正する〉→〈歪ませてから補正する〉と切り替える | 〈補正する〉で線が**逆向き**に曲がる。〈歪ませてから補正する〉でほぼ元の格子に戻るが、**PSNR は ∞ にならない**。〈格子を重ねる〉を外して k₁ を −0.1 → −0.4 と強めると、**PSNR が下がっていく**（33 dB 前後 → 29 dB 前後） |

**同好会で見せるなら 38・40・41 が山場です。**

- **38** … 補間の違いが見える一番の場面。ルーペを当てて見せる
- **40** … 「90° 回転は何度やっても劣化しない」ことの実演（PSNR が ∞ になる）
- **41** … 樽型と糸巻き型の見分け。格子の曲がり方が逆になる

### 42 の数値を読むときの注意

**〈格子を重ねる〉を外してください。**1画素幅の線は2回の再標本化で真っ先に壊れるので、
格子を入れたままだと PSNR が 20 dB 前後まで落ち、歪みの強さの違いが読めなくなります。

また、この往復の数値では**補間の順位が絵によって入れ替わります**。
`siemens` のような細かい絵ではバイキュービックが最良ですが、
`lowcontrast` のようになだらかな絵では**最近傍が一番高く**出ます
（k₁ = −0.25 で最近傍 44.0 dB 対 バイリニア 39.7 dB）。
なだらかな絵では、最近傍のずれによる誤差より、バイリニアのぼかしによる誤差のほうが
大きくなるためで、**正常な動き**です。

---

## 確認項目 45〜48 ── 周期ノイズの付加（2026-09-10 に全項目合格ずみ）

以下は記録です。**やり直す必要はありません。**

**なぜ足したか。**同好会の山場「スペクトルマスクで縞だけを抜く」を、
**手持ちの写真で成立させる**ためです。2026-09-09 に実写で3回試して、
全部だめでした（印刷物の複写・パソコン画面・網戸ごしの風景）。

| 試したもの | なぜだめだったか |
|---|---|
| パソコン画面の複写 | 画素ピッチが細かすぎてナイキスト際。折り返してモアレになり、周期が一定でない |
| 網戸ごしの風景 | あおって撮るので遠近で目の間隔が変わり、**成分が帯に散る**。最適な塗り方でも**網の 6.5% しか落ちない**（輝点は全パワーの 0.38%、網の帯は 2.21%） |

**合成の縞ならこうなりません。**そこで「わざと乗せてから消す」道を用意しました。
実演としてはむしろ明快で、**乗せた本人が消す**ので因果がはっきり見えます。

### 前提：この4項目だけ、途中でファイルを1回はさみます

op は1つずつしか掛けられないので、**縞を乗せた絵を PNG で保存し、読み込み直してから**
スペクトルマスクを開きます。**本番では、縞入り版を先に作って置いておいてください。**

### 塗るときの設定（ここが要）

**「筆の太さ」を 8、「マスクの縁のぼかし」を 1 にしてください。**
既定の「筆5・ぼかし2」では落ち切りません。640×480 の実測：

| 筆 / ぼかし | 塗ったあとの PSNR |
|---|---:|
| 5 / 2（既定） | 27.0 dB ← **足りない** |
| 5 / 0 | 41.9 dB |
| **8 / 1** | **43.6 dB** ← 推奨 |
| 12 / 2 | 45.1 dB |

（縞あり 21.6 dB。ぼかし σ=2.5 で消そうとすると 26.1 dB にしかならず、絵も潰れます）

**ぼかしは筆より小さく**が原則です。同じか大きいとマスクの谷が浅くなり、半分しか落ちません。

### サンプルの選び方（2026-09-10 に一度まちがえました）

**白飛び・黒つぶれしやすい絵は使えません。**背景が明るい絵に振幅30の縞を足すと
255 で頭打ちになり、サインの山が刈り取られて波形が歪みます。歪んだ波は
**基本波の2倍の位置にも輝点を作る（倍音）**ので、印が6個になり、
しかも**飽和で失われた情報は塗っても戻りません**。

| サンプル | 飽和した画素 | 輝点 | 塗ったあと |
|---|---:|---:|---:|
| `geometry`（背景242・図形28） | **31.7 %** | **6個**（倍音つき） | 33.9 dB |
| `siemens` | 18.3 % | 2個 | 34.4 dB |
| `zone` | 13.0 % | 2個 | 33.7 dB |
| **`gaussnoise`** | **2.1 %** | **4個** | **40.6 dB** ← これを使う |
| `lowcontrast` | 0.0 % | 6個（うち2個はこの絵が元から持つ模様） | 43.5 dB |

**最初この4項目を `geometry` で書いていました**が、上の表のとおり最悪の選択でした。
セルフテストには「飽和すると高調波が出る」と書いておきながら、
チェックリストのサンプル選びに反映していなかったためです（この文書の冒頭の教訓そのもの）。

| # | 開き方 | 操作 | 合格の見え方 |
|---|---|---|---|
| **45** | #op=noise&sample=gaussnoise | 「種類」を**周期ノイズ（斜め縞）**に変える | **右上がりの斜めの縞**が絵の全面に乗る。右下の PSNR が **21〜22 dB** 前後。「縞の間隔」「縞の向き」「縞の濃さ」の3本が現れ、**「乱数の種」と「色ごとに独立に乗せる」は消える**（乱数を使わないため） |
| **46** | （45のまま） | 「縞の間隔」を 3 → 30 と動かす。次に「縞の向き」を 27 → 90 → 150 と動かす。「縞の濃さ」を 5 → 60 | 間隔で縞の粗さが、向きで傾きが、濃さで見えやすさが変わる。**90° にすると横縞になる**（このあと45〜48 を続けるときは **27° に戻し、濃さも 30 に戻してください**） |
| **47** | （45のまま。既定の 5画素・27°・濃さ30） | **［PNG保存］** を押し、保存された PNG を **［画像を開く］で読み込み直す**。左のリストで**〈スペクトルマスク（手で塗る）〉** | 右パネルに**黄色い破線の丸が4つ**出て、**4つとも中心から同じ距離**にある。**強い1組（「ここ」と出るほう）が本物**で、弱いもう1組は鏡映で埋めた副産物です。項目46 で向きを 90° にしたままだと**1つも出ません**（十字の上に乗るため） |
| **48** | （47のまま） | **「筆の太さ」を 8、「マスクの縁のぼかし」を 1** にしてから、**「ここ」と出ている丸**とその点対称の相方を塗る（＝**強い1組だけ**。外側の丸はこの絵では出ません） | **斜めの縞だけが消えて、絵はぼけない。**キー `3` ⇄ `2` で往復すると、絵の細部が残っているのが分かる。キー `4`（差分表示）にすると**縞模様だけが差分に出る**（図形の輪郭のまわりに波紋が出ていたら、それは飽和した絵を使っている合図です）。ぼかしを 2 に戻すと**縞が半分残る**ことも確かめてください（この設定が要る理由です） |

### おまけ：わざと白飛びさせてみる（教材として面白いところ）

**#op=noise&sample=geometry** で同じことをすると、**印が6個**になります。
外側の2個は**基本波のちょうど2.00倍**の位置で、**倍音**です。
白い紙（242）に振幅30の縞を足すと 272 になって 255 で頭打ちになり、
**サインの山が平らに刈り取られる**——その歪みが倍音を生みます。
音でいえば、アンプを歪ませると倍音が乗って音色が変わるのと同じことです。

倍音まで全部塗っても **33.9 dB** までしか戻りません（`gaussnoise` は 40.6 dB）。
**飽和で刈り取られた情報は、どこにも残っていないから**です。

---

## とくに見ていただきたいところ

- **29**（今回の再確認） … 「計算時間」で四角・十字・円を比べたとき、
  **円だけが5〜6倍**になっているか。**同じ形で何度か切り替えて、数字が落ち着くか**
- **31・33・35**（デモザイク） … 上記のとおり
- **38・40・41**（幾何変換） … 上記のとおり
- **45〜48**（周期ノイズ） … 2026-09-10 に合格。**48 の差分表示が一面の斜め縞だけになった**

### 45〜48 の結果（2026-09-10）

**`gaussnoise` で通したあと、写真（`samples/C少し眠い・甘い写真.jpg`）でも確かめました。**

| | 輝点 | 縞あり | 塗ったあと |
|---|---:|---:|---:|
| `gaussnoise` | 4個（すべて同じ半径） | 21.7 dB | 40.6 dB |
| 写真C | 4個（すべて同じ半径） | 21.8 dB | **38.6 dB** |

**差分表示（キー4）が一面の斜め縞だけになる**のが合格の姿です。
図形の輪郭のまわりに波紋が出ていたら、それは飽和した絵を使っている合図。

**実演用に、写真Cの縞入り版を PNG で作って手元に置いてあります**
（`samples/D縞入り.png` と `samples/D縞入り風景.png`。**公開版には同梱していません**）。
これを［画像を開く］で読み込めば、PNG保存と読み込み直しを省いて
すぐ〈スペクトルマスク〉から始められます。

**ぼかしは 1。**写真Cで **ぼかし1 が 38.6 dB、ぼかし2 は 35.1 dB**。
また**強い1組だけ塗るほうが、4つ全部塗るより良い**（38.6 対 37.7 dB）。

## おまけ：ここまで通ったら試すと面白いところ

チェックではありませんが、同好会で見せる想定の並びです。

1. #op=fft&sample=geometry … 直線に対して**直角**の向きに輝線が伸びる
2. #op=fftfilter&sample=periodic で「種類」をノッチにし、u = 0.18 / v = 0.09 に合わせる
   … 5 と同じことを**式で**やる。手（5）と式（4→9）の両方を見せると話がつながります
3. #op=fftmask&sample=periodic で輝点を塗ってから「反転」を入れる
   … 消したはずの**縞だけ**が取り出される
4. #op=denoisecmp&sample=siemens で「ガウシアンの σ」を 0.5 から 5 まで上げていく
   … **PSNR が頭打ちになって下がりはじめたあとも、SSIM はしばらく伸び続ける**。
   「どこまでぼかすのが最適か」の答えが物差しによって違う、という話になります。
   （どの σ で折り返すかは絵によって変わります。`geometry` では線が細いので、
   ガウシアンは最初から損をしつづけます）
5. #op=noise&sample=step で種類を〈ショット〉にし、「最大光子数」を 10 まで下げる
   … 暗い段だけが荒れる。光子数のばらつきが由来だと目で分かります
6. #op=median&sample=saltpepper で「実行」欄を見てから、#op=morphology&sample=saltpepper
   と見比べる … **並列できる処理（Worker が 4〜8 並列）と、できない処理（Worker 1 並列）**の
   差が「計算時間」と「画面に出るまで」の開き方に出ます（前者は計算時間のほうが大きく、
   後者は2つがほぼ同じになります）

---

## 次にここへ項目を足す人へ

**44項目はすべて合格ずみです。**処理を足したときにここへ項目を追加する場合、
今回踏んだ落とし穴を繰り返さないようお願いします。

1. **期待値は書く前に Node で数値の裏を取る。**
   「見えるはず」と思って書いた項目のうち **4件**が、実際には見えませんでした。
   op は正しく、私の書いた手順が誤っていたケースです（上の表）。
2. **効果を見せる項目は、効果が大きく出るサンプルを数値で選ぶ。**
   ノイズの無い絵でノイズ除去を見せようとしたり（34）、
   半分に縮んだ表示で1画素の粒を見せようとしたり（31）していました。
3. **専門用語は初出で意味を書く。**「タップ数」を説明なしに使ってしまいました
   （2026-09-07 に用語欄を追加）。
4. **「開き方」の欄はバッククォートを使わず素の文字列で書く。**
   `%60` が URL に混ざって別のサンプルが開きます。
5. **数字を報告してもらうときは「実行」欄もセットで。**
   経路（メインスレッド／Worker）が違う数字を並べると読み違えます。
   実際 2026-09-06 に、メインスレッドの 192 ms と Worker の 15 ms を
   「同じ計算で13倍」と書いてしまい、画面確認で指摘を受けました。
